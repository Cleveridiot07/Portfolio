export type TypeSkills= {
    skillName: string;
    skillLevel: string;
    iconPath: string;
}

export type TypeSkillCard={
    oddEven : boolean;
    skillName: string;
    iconPath : string;
    skillLevel: string;
}